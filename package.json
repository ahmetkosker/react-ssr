{
  "name": "react-ssr",
  "packageManager": "yarn@4.0.2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "esbuild": "^0.19.11",
    "express": "^4.18.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/compression": "^1",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.5",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "nodemon": "^3.0.2",
    "tailwindcss": "^3.4.0",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "start": "nodemon build/server.js --watch",
    "build:client": "esbuild src/index.js --bundle --minify --loader:.js=jsx --outfile=dist/bundle.js",
    "build:server": "esbuild server/server.tsx --bundle --platform=node --outfile=build/server.js",
    "build": "yarn build:client && yarn build:server",
    "tailwindcssWatch": "yarn run tailwindcss -i ./style/index.css -o ./dist/bundle.css --watch"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
